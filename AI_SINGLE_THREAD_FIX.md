# AI Auto Response - Однопоточная архитектура

## 🔴 ПРОБЛЕМЫ:

1. **`.data` вместо `.list`** - ОШИБКА! 
   - `.data` = кэш всех профилей
   - `.list` = чаты ТЕКУЩЕГО активного профиля
   
2. **Параллельная обработка** - профили обрабатываются одновременно → конфликты при переключении

3. **Нет остановки после ответа** - продолжает искать даже после отправки

## ✅ ПРАВИЛЬНАЯ ЛОГИКА:

### 1. Цикл по профилям (ПОСЛЕДОВАТЕЛЬНО):
```
ДЛЯ КАЖДОГО профиля:
  ЕСЛИ newMessages > 0:
    - Переключиться на профиль
    - Ждать 2-3 сек загрузки
    - 5 попыток найти unanswered в modelsChat.getChats.list
    - ЕСЛИ нашёл → отправить через 23-30 сек → ВЫХОД из цикла
  ИНАЧЕ:
    - Переключиться на профиль  
    - Ждать 2-3 сек загрузки
    - 1 попытка найти unanswered в modelsChat.getChats.list
    - ЕСЛИ нашёл → отправить через 23-30 сек → ВЫХОД из цикла
```

### 2. Ключевые моменты:
- ✅ **Однопоточно** - только 1 профиль обрабатывается
- ✅ **`.list`** - чаты текущего профиля после переключения
- ✅ **СТОП** после любой отправки
- ✅ **Задержка** 2-3 сек после переключения
- ✅ **Отправка** через 23-30 сек

## 📝 ПЛАН:

1. Вернуть `.list` вместо `.data`
2. Переписать `processAccountMessages` под однопоточную модель
3. Добавить `break` после планирования отправки
4. Использовать логику как в `chatExtractor.js` (строки 71-131)
